context:
  version: "0.21"

package:
  name: otmixmod
  version: ${{ version }}

source:
  url: https://github.com/openturns/otmixmod/archive/v${{ version }}.tar.gz
  sha256: 7523b95e146aaf67938c75ffe62e934321fa591c9e5405a443b732bbd3a1db73
  patches:
    - 0001-Export-symbols.patch

build:
  number: 0
  python:
    version_independent: true

requirements:
  build:
    - cmake
    - swig
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - if: unix
      then: make
      else: ninja
    - if: win
      then: m2-diffutils
    - if: build_platform != target_platform
      then:
        - python-abi3 ${{ python_min }}.*
        - python ${{ python_min }}.*
        - cross-python_${{ target_platform }}
  host:
    - python-abi3 ${{ python_min }}.*
    - python ${{ python_min }}.*
    - openturns
    - libmixmod
  run:
    - python >=${{ python_min }}
  run_exports:
    - ${{ pin_subpackage('otmixmod', upper_bound='x.x') }}

tests:
  - python:
      imports:
        - otmixmod

about:
  homepage: http://openturns.org/
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: COPYING
  summary: MixMod module
  documentation: https://openturns.github.io/otmixmod/master/
  repository: https://github.com/openturns/otmixmod

extra:
  recipe-maintainers:
    - jschueller
